package wasi:filesystem@0.2.0;

interface preopens {
    use file-types.{descriptor};

    /// Return the set of preopened directories, and their paths.
    get-directories: func() -> list<tuple<descriptor, string>>;
}
